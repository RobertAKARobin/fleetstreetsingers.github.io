# Build settings
include: 
  - _pages
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/
plugins:
  - jekyll-tidy
markdown: kramdown
google_analytics: true
highlighter: rouge
kramdown:
  syntax_highlighter_opts:
    disable: true
sass:
  style: expanded
defaults:
  -
    scope:
      path: ""
    values:
      layout: "base"